import InfoPage from "@/components/support/InfoPage";
import { termsContent } from "@/data/support-pages";

export default function KullanimKosullariPage() {
  return <InfoPage content={termsContent} />;
}
